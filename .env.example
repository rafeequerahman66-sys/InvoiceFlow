# Postgres (Supabase: Project Settings -> Database -> Connection string -> URI)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/postgres?sslmode=require"

# Auth.js v5
AUTH_SECRET="run: npx auth secret"
AUTH_GOOGLE_ID="your-google-oauth-client-id"
AUTH_GOOGLE_SECRET="your-google-oauth-client-secret"

# Comma-separated allowlist of people who may sign in (you + Ismaeel)
ALLOWED_EMAILS="rafeequerahman66@gmail.com"

# Your existing WeasyPrint render service (optional; PDF route 501s until set)
WEASYPRINT_URL="http://localhost:8001/render"
